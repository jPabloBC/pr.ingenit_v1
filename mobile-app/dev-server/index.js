// NOTE: dev-server REMOVED
// The local dev auth server that used to be here was removed because it's not required for
// development. Keeping an executable server in the mobile app folder caused confusion.
//
// If you see this file it means it was intentionally neutralized. Do NOT execute it.
// To restore a development auth server, prefer creating a separate repository or script
// under `scripts/` and document required environment variables (like JWT_SECRET).

module.exports = {} // placeholder to avoid require-time errors
